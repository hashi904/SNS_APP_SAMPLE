.right-side
  .right-side-user
    .right-side-user__title
      おすすめのユーザー
    - @recommend_users.each do |user|
      .right-side-user__content
        .right-side-user__content-image
          = link_to "/users/#{user.id}", method: :get do
            - if user.image.present?
              = image_tag(user.image.url, class: 'user-image')
            - else
              = image_tag('member_photo_noimage.png', class: 'user-image')
        .right-side-user__content-info
          .right-side-user__content-info-name
            = user.nickname
          .right-side-user__content-info-username
            = user.username
        = render partial: "/relationships/follow_btn", locals: { user: user}
    .right-side-user
      = link_to "さらに表示", "/users", class: "right-side-user", method: :get
